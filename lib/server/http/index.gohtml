<html lang="en-US">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width"/>
    <title>Housesitter for Zigbee2MQTT</title>
    <link href="/public/main.css" rel="stylesheet">
    <script>
      function openpanel() {
        const panel = document.querySelector("#panel");
        panel.classList.toggle("hidden")
        // panel.scrollIntoView()
      }
    </script>
</head>
<body>

<header>
    <h1>
        Housesitter for Zigbee2MQTT
    </h1>
</header>
<section id="schedule-grid">
    <article class="schedule-box">
        {{ range .Legends }}
            <div class="{{.DisplayClasses }}" {{.Style}}>
                {{ .Title }}
            </div>
        {{ end }}
        {{ range .Devices }}
            <div class="label chamfer"
                    {{ .InlineStyles }}
            >
                {{ .FriendlyName }}
            </div>
            {{ range .Schedules}}
                <div {{.InlineStyles }} class="schedule chamfer" onclick="openpanel()">
                    {{ .Title }}
                </div>
            {{ end }}
        {{ end }}
    </article>
</section>
<article id="panel" class="hidden">
    <em>stuff here!</em>
    put here a list of all the schedules, and you can page through it and edit them.
</article>
</body>
</html>
